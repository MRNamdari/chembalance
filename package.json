{
  "name": "chem_eq_balancer",
  "version": "1.0.0",
  "description": "offline chemical equation balancer",
  "main": "dist/index.html",
  "scripts": {
    "test": "bun build ./src/index.ts --sourcemap=inline --target=browser --outdir ./dist/ --watch "
  },
  "author": "mo.re.nam",
  "license": "ISC",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "typescript": "^5.3.3"
  }
}
